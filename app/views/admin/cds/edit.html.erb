<h1>商品編集ページ</h1>
<%= form_for(@user) do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>
    <h4>本文</h4>
    <%= f.text_area :body %>
    <input type="submit" name="commit" value="保存" data-disable-with="保存" class="btn btn-primary btn-lg active">
                
    <% end %>